// Take integer as input and print half of the number.
#include <stdio.h>
int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    printf("Half of %d is %d\n", num, num / 2);
    return 0;
}